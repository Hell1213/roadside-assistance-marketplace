name: shared
description: "Shared design system and utilities for Roadside Assistance Marketplace"
version: 1.0.0+1

environment:
  sdk: ^3.10.4
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  # Fonts will be added once Gilroy font files are available
  # For now, apps will use system default fonts